# Documentation site dependencies (MkDocs + Material).
# Install with:  pip install -r docs/requirements.txt
# Serve locally: mkdocs serve     Build: mkdocs build --strict
mkdocs-material>=9.5,<10
pymdown-extensions>=10.7
